# Environment variables example
# Rename this file to .env and fill in your actual values
# Do NOT commit the .env file to version control

# Server Configuration
NODE_ENV=development # or production
PORT=5000

# MongoDB Connection
MONGO_URI=your_mongodb_connection_string

# JWT Configuration
JWT_SECRET=your_very_strong_jwt_secret # Should be long and random
JWT_EXPIRES_IN=30d # Token expiration time

# PayPal Configuration
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_APP_SECRET=your_paypal_app_secret # If needed for backend operations
PAYPAL_API_BASE=https://api-m.sandbox.paypal.com # Or production URL

# Other potential variables
# CORS_ORIGIN=http://localhost:3000 # Frontend URL for CORS
